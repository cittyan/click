# 构建 wheel

## 通过源码构建 wheel

- 安装依赖:

```shell
pip install --upgrade pip build wheel
```

- 项目根目录构建:

```shell
python -m build --wheel
```

- 项目根目录生成 .whl 文件。 dist/click-8.4.1-py3-none-any.whl

- 本地安装和强制安装 .whl 文件

```shell
pip install dist/click-8.4.1-py3-none-any.whl
pip install --force-reinstall dist/click-8.4.1-py3-none-any.whl
```

# 执行测试用例

- 安装依赖:

```shell
pip install pytest
```

- 配置 .venv 虚拟环境:

```shell
python -m venv .venv
```

-  进入 .venv 虚拟环境:

```shell
source venv/bin/activate       # Linux/macOS
.\.venv\Scripts\activate       # (Windows)
```

- 执行用例:

```shell
pytest tests/

pytest tests/test_xxx.py
```

- 退出虚拟环境:

```shell
deactivate
```

# 翻译

## 前置条件准备

安装 babel

```shell
pip install babel
```

项目目录结构:

```text
your_project/
├── babel.cfg              # Babel 提取配置
├── your_app/              # 你的 Click 源码
│   ├── __init__.py
│   └── cli.py
├── translations/          # 翻译存放根目录（手动创建）
└── i18n.sh                # 自动化脚本（可选）
```

项目根目录创建 babel.cfg 配置文件:

```shell
[python: **.py]
encoding = utf-8
```

## 初次创建 zh_CN 翻译

- 项目根目录执行，扫描当前目录及所有子目录的 *.py 文件，提取所有待翻译字符串，生成 POT 模板:

```shell
pybabel extract -F babel.cfg -o messages.pot .
```

- 为 zh_CN 特定语言初始化 PO 文件:

```shell
pybabel init -i messages.pot -d translations -l zh_CN

# -d translations：翻译文件存放目录。
# -l zh_CN：语言代码（简体中文）。
# 执行后生成：translations/zh_CN/LC_MESSAGES/messages.po
```

- 打开 translations/zh_CN/LC_MESSAGES/messages.po，将每个 msgid 对应的 msgstr "" 改为中文翻译
- 编译 PO 为 MO 文件:

```shell
pybabel compile -d translations

# 此命令会为所有语言目录下的 messages.po 生成对应的 messages.mo，运行时实际加载的是 .mo
```

## 后续更新翻译的操作

- 重新提取最新的 POT:

```shell
pybabel extract -F babel.cfg -o messages.pot .
```

- 合并到已有的 PO 中（安全更新）:

```shell
pybabel update -i messages.pot -d translations -l zh_CN
```

- 编辑更新后的 PO
- 重新编译:

```shell
pybabel compile -d translations
```

## 注意事项

1. 翻译域（domain） 必须与 .mo 文件名一致（这里是 messages）。
2. 目录结构 非常重要：translations/<语言>/LC_MESSAGES/messages.mo 缺一不可。
3. 环境变量 可通过 LANGUAGE、LANG、LC_ALL 指定语言，gettext 会按优先级查找。
4. 如果官方 click 源码本身也采用了 _() 标记，你的应用加载翻译后，其内置字符串（如 Usage:）也会被你的翻译覆盖。
5. 若出现乱码或翻译不生效，检查 .mo 文件是否编译正确、文件路径是否正确、系统 locale 是否安装了对应语言包（Linux 下可能需要安装 language-pack-zh-hans 等）。
